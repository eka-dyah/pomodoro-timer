const LayoutControlTime = ({children}) => (
	<div className="col-12 mb-4 mt-md-3">
		<div className="row d-flex justify-content-center">
            {children}
        </div>
	</div>
);

export default LayoutControlTime;
